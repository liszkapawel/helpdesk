nelmio_api_doc:
    documentation:
        info:
            title: Ticketerr API
            description: API systemu obsługi zgłoszeń Ticketerr
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api
